# Endpoint usado por Streamlit para comunicarse con FastAPI
# Local (sin Docker): http://localhost:8000/predict
# Docker Compose (interno): http://backend:8000/predict
BACKEND_PREDICT_URL=http://localhost:8000/predict
